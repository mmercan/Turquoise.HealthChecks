# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: Turquoise.HealthChecks.Network

on:
  push:
    paths:
      - 'HealthChecks/Turquoise.HealthChecks.Network/**'
  pull_request:
    paths:
      - 'HealthChecks/Turquoise.HealthChecks.Network/**'

jobs:
  call-workflow-passing-data:
    uses: mmercan/Turquoise.HealthChecks/.github/workflows/reusable-buildPack-workflow.yml@master
    with:
      working-directory: ./HealthChecks/Turquoise.HealthChecks.Network
    secrets:
      NugetApiKey: ${{ secrets.NUGETAPIKEY }}