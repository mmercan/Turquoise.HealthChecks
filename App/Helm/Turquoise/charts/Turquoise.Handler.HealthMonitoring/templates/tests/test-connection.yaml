apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Turquoise.Handler.HealthMonitoring.fullname" . }}-test-connection"
  labels:
    {{- include "Turquoise.Handler.HealthMonitoring.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Turquoise.Handler.HealthMonitoring.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
