apiVersion: v1
kind: Service
metadata:
  name: {{ include "Turquoise.Handler.HealthMonitoring.fullname" . }}
  labels:
    {{- include "Turquoise.Handler.HealthMonitoring.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Turquoise.Handler.HealthMonitoring.selectorLabels" . | nindent 4 }}
