apiVersion: v1
kind: Service
metadata:
  name: {{ include "turquoise-redis.fullname" . }}
  labels:
    {{- include "turquoise-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "turquoise-redis.selectorLabels" . | nindent 4 }}
